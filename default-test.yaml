# .cloudbuild/pipeline.yaml

workspaces:
- name: default-workspace
params:
- name: repository
  value: $(params.tkn-repo)
tasks:
- name: pipeline-task
  taskSpec:
    steps:
    - image: busybox
      script: echo "Hello World!"
